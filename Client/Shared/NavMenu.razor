<nav>
    <div class="logo-container">
        <img src="/images/logo.png" alt="Logo" class="logo">
    </div>
    <div class="nav-links">
        <NavLink href="" Match="NavLinkMatch.All" class="nav-link">
            משחק
        </NavLink>
        <NavLink href="GamesList" class="nav-link">
            עורך
        </NavLink>
        <NavLink href="About" class="nav-link">
            אודות
        </NavLink>
    </div>
</nav>

<style>
    .nav-links {
        display: flex;
        gap: 30px; /* הגדלת הרווחים בין הפריטים */
    }

        .nav-links .nav-link {
            text-decoration: none !important;
            color: white !important; /* צבע טקסט לבן עם חשיבות גבוהה */
            padding: 10px 15px; /* הוספת padding קבוע */
            transition: all 0.3s;
            position: relative; /* כדי לאפשר קו תחתון */
        }

            .nav-links .nav-link::after {
                content: '';
                position: absolute;
                width: 0;
                height: 2px;
                bottom: 0;
                left: 0;
                background-color: white !important; /* קו תחתון לבן */
                transition: width 0.3s ease-out; /* אנימציה של הקו */
            }

            .nav-links .nav-link:hover::after, .nav-links .nav-link:focus::after {
                width: 100% !important; /* הגדל את הקו התחתון על מעבר העכבר */
            }

            .nav-links .nav-link.active {
                color: white !important;
            }

            .nav-links .nav-link:hover, .nav-links .nav-link:focus {
                color: white !important; /* ודא שהצבע נשאר לבן במעבר עכבר */
                text-decoration: none !important; /* הסר כל קו תחתון כברירת מחדל */
            }
</style>

@code {

}
